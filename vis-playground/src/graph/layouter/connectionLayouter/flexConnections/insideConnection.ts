import { CombinedPathSegment } from "src/graph/graphical/primitives/pathSegments/PathSegment";
import { FlexConnection } from "./flexConnection";




export class InsideParentConnection extends FlexConnection {

    override init(): void {

        // Add the node to the fitting continuum of the source and target flex nodes

        this.flexNode


    }

    override calculate(): void {

    }
}
